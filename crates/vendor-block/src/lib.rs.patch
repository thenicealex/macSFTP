--- src/lib.rs
+++ src/lib.rs
@@ -1,1 +1,2 @@
+#![allow(missing_abi)]
 /*!
@@ -54,7 +55,8 @@
 use std::os::raw::{c_int, c_ulong, c_void};
 use std::ptr;
 
-enum Class { }
+#[repr(C)]
+struct Class { _priv: [u8; 0] }
 
 #[cfg_attr(any(target_os = "macos", target_os = "ios"),
            link(name = "System", kind = "dylib"))]
