# macSFTP 0.2.0 release evidence

Version: 0.2.0
Status: PASS
Tester: thenicealex (native GUI smoke on local arm64); GitHub Actions release gates
Date: 2026-08-24
Supported architectures: arm64
Minimum macOS tested: macOS 15 arm64 in CI; Info.plist declares macOS 13.0
Latest supported macOS tested: macOS 27.0 arm64 in native GUI smoke

Use only local fixtures or non-sensitive test servers in screenshots and notes.

Automated gates (2026-08-24, this branch):

- [x] `bash scripts/check.sh` (fmt, architecture, sensitive-log gates,
      workspace tests across 15 suites, clippy -D warnings)
- [x] Password-authentication Docker gate (GitHub Actions "Password
      authentication" on PR #22)
- [x] Isolated macOS Keychain gate (GitHub Actions on PR #22)
- [x] `bash scripts/build_app.sh` and `plutil` validation
      (CFBundleShortVersionString 0.2.0, local arm64)
- [x] App launches from the generated bundle
- [x] Native menus, About, Settings, and version display (About reports 0.2.0)
- [x] Two windows preserve independent tabs and paths after quit/relaunch
- [x] Closing windows in either order does not discard another window's state
- [x] Password and Ed25519/ECDSA private-key connections
- [x] Unknown host acceptance and host-key mismatch blocking
- [x] Upload, download, conflict, cancel, and retry
- [x] 10k entries, filtering, tab switching, and four active transfers
- [x] 720x480, Retina, light/dark themes, keyboard navigation, basic VoiceOver
- [x] Log inspection contains no credentials, fingerprints, or private-key paths

Notes:

- This release carries the profiles-audit fix cycle (batches A-F):
  storage hardening with an advisory-lock write path, the private-key
  passphrase tri-state (profiles.json v2 -> v3 in-place migration),
  deletion reference decoupling, cross-instance write serialization for
  recents/config/residual stores, and connection-keyed remote-edit
  identity. Details and deliberately-declined work:
  `docs/plans/2026-08-22-profiles-audit-fix-cycle.md`.
- Native GUI smoke specifically covered the passphrase policy toggle in
  all three states (no passphrase / ask every time / remember) and the
  in-place migration of a pre-0.2.0 profiles.json file.
- The public GitHub release contains source archives only. The generated
  app bundle remains unsigned and is not attached as a distributable
  binary.
