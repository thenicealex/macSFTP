# macSFTP 0.1.0 release evidence

Version: 0.1.0
Status: IN PROGRESS
Tester: Codex local automated and native GUI validation
Date: 2026-07-15
Supported architectures: arm64 tested; product support decision pending
Minimum macOS tested: TBD
Latest supported macOS tested: 27.0 arm64

Use only local fixtures or non-sensitive test servers in screenshots and notes.

- [x] `bash scripts/check.sh`
- [ ] `bash scripts/test_password_auth.sh`
- [x] `bash scripts/test_keychain.sh`
- [x] `bash scripts/build_app.sh` and `plutil` validation
- [x] App launches from the generated bundle
- [x] Native menus, About, Settings, and version display
- [ ] Two windows preserve independent tabs and paths after quit/relaunch
- [ ] Closing windows in either order does not discard another window's state
- [ ] Password and Ed25519/ECDSA private-key connections
- [ ] Unknown host acceptance and host-key mismatch blocking
- [ ] Upload, download, conflict, cancel, and retry
- [ ] 10k entries, filtering, tab switching, and four active transfers
- [ ] 720x480, Retina, light/dark themes, keyboard navigation, basic VoiceOver
- [ ] Log inspection contains no credentials, fingerprints, or private-key paths

Notes:

- Application signing and notarization are intentionally outside the 0.1.0
  source-readiness gate. Do not publish the unsigned bundle as a public release.
- Native GUI inspection confirmed two simultaneous windows in the Window menu,
  About version 0.1.0, Settings rendering, and the unsigned arm64 bundle launch.
  Restore/close-order testing remains unchecked because this pass did not close
  live windows or mark incomplete interaction coverage as passed.
- The sandboxed bundle build used the deterministic ICNS fallback after
  `iconutil` reported `Invalid Iconset`. The resulting ICNS is recognized by
  macOS tooling, but native CI must confirm the primary `iconutil` path before
  release evidence can pass.
