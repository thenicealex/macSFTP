# macSFTP 0.1.1 release evidence

Version: 0.1.1
Status: PASS
Tester: Codex automated validation and native GPUI visual QA; GitHub Actions release gates
Date: 2026-08-05
Supported architectures: arm64 tested in GitHub Actions and native visual QA
Minimum macOS tested: macOS 15 arm64 in CI; Info.plist declares macOS 13.0
Latest supported macOS tested: macOS 27.0 arm64 in native visual QA

Use only local fixtures or non-sensitive test servers in screenshots and notes.

- [x] `cargo test -p macsftp-app` (201 tests)
- [x] `cargo clippy -p macsftp-app -- -D warnings`
- [x] GitHub Actions quality and integration tests
- [x] GitHub Actions dependency policy gate
- [x] Docker-backed password-authentication gate
- [x] Isolated macOS Keychain gate
- [x] App bundle build and `plutil` validation in GitHub Actions
- [x] Delete confirmation dialog rendered in One Light and One Dark
- [x] `bash scripts/check_release.sh v0.1.1`

Notes:

- The release changes only the delete confirmation presentation and its
  presentation-model tests. Delete dispatch, recursive-delete behavior, and
  configuration semantics are unchanged.
- Native GPUI visual QA covered a single-folder recursive-delete confirmation
  in One Light and One Dark. The dialog used responsive width constraints and
  truncated long item names; mixed selections and preview overflow are covered
  by unit tests.
- The public GitHub release contains source archives only. The generated app
  bundle remains unsigned and is not attached as a distributable binary.
