[toolchain]
# Keep this exact version aligned with .github/workflows/ci.yml. Cargo.toml's
# rust-version remains the MSRV gate for dependency resolution.
channel = "1.96.1"
components = ["clippy", "rustfmt"]
targets = ["aarch64-apple-darwin"]
